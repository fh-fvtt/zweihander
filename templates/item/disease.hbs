<div class="form-group">
  <label>Resist</label>
  <input name="data.resist" type="text" value="{{data.resist}}">
</div>

<div class="form-group">
  <label>Duration</label>
  <input name="data.duration" type="text" value="{{data.duration}}">
</div>

<div class="form-group stacked">
  <label><a class="open-editor">Effect <i class="fa fa-edit"></i></a></label>
  <div class="zh-editor-preview open">{{{zhProcessRuleText (zhLocalize data.rules.effect)}}}</div>
  {{editor content=(zhLocalize data.rules.effect) target=(zhLocalizePath "data.rules.effect") button=true owner=owner
  editable=editable}}
</div>

<div class="form-group stacked">
  <label><a class="open-editor">Treatment <i class="fa fa-edit"></i></a></label>
  <div class="zh-editor-preview open">{{{zhProcessRuleText (zhLocalize data.rules.treatment)}}}</div>
  {{editor content=(zhLocalize data.rules.treatment) target=(zhLocalizePath "data.rules.treatment") button=true owner=owner
  editable=editable}}
</div>

<div class="form-group stacked">
  <label>Status</label>
  <label class="checkbox">
    <input name="data.active" type="checkbox" class="link-checkbox" {{checked data.active}} />
    Currently in Effect
  </label>
</div>