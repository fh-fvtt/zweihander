<section class="item-card">
    <h2 class="item-card-title">{{name}}</h2>
    <div class="item-card-description">
      {{{zhProcessRuleText data.flavor.description}}}
    </div>
    <dl class="compact-details zh-collapsed zh-expandable" data-expand-title="Show Item Details">
      <dt>Associated Skill</dt>
      <dd>{{data.associatedSkill}}</dd>
      <dt>Load</dt>
      <dd>{{data.load}}</dd>
      <dt>Handling</dt>
      <dd>{{data.handling}}</dd>
      <dt>Distance</dt>
      <dd>{{data.distance}}</dd>
      <dt>Type</dt>
      <dd>{{data.weaponType}}</dd>
      <dt>Encumbrance</dt>
      <dd>{{data.encumbrance}}</dd>
      <dt>Price</dt>
      <dd>
        {{zhPrice data.price}}
      </dd>
    </dl>
    <dl class="item-card-qualities zh-expandable zh-collapsed" data-expand-title="Show Qualities">
      {{#each data.qualities}}
      <dt>{{name}}</dt>
      <dd>{{effect}}</dd>
      {{/each}}
    </dl>
  </section>